apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bootaeva-com-cert
  namespace: ingress-nginx
spec:
  dnsNames:
    - "*.bootaeva.com"
    - "bootaeva.com"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: bootaeva-com-tls